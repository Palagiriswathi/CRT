#patterns
#multiple[outerloop,inner loop]
#-->outerfor loop indicates no of rows
#-->innerfor loop indicates no of colums

n=int(input())
for row in range(n):
    for col in range(n):
        print("*",end=" ")
                   